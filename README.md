# ARM Sales - Система управления продажами

## 🚀 Быстрый старт (Локально)

1. Клонируйте репозиторий.
2. Создайте `laravel/.env` на основе `laravel/.env.example`.
3. **Для работы очередей** добавьте в `laravel/.env`:
   ```env
   QUEUE_CONNECTION=database
   ```
4. Запустите проект:
   ```bash
   docker-compose up -d
   ```
5. Установите зависимости:
   ```bash
   docker-compose exec php composer install
   docker-compose exec php php artisan key:generate
   docker-compose exec php php artisan migrate --seed
   ```

### 🔄 Запуск воркера очередей (опционально)

Для обработки фоновых задач (AI отчеты, синхронизация BlueSales):

```bash
# Удалить старые контейнеры воркеров (если были)
docker rm -f arm_queue_default arm_queue_ai arm_queue_sync arm_cron 2>/dev/null || true

# Запустить воркер очередей
docker-compose --profile workers up -d

# Остановить воркер
docker-compose --profile workers down

# Просмотр логов
docker logs -f arm_default_queue_worker
```

---

## 🌐 Настройка Production (Beget / VPS)

### 1. Настройка Nginx и SSL (HTTPS)
Для работы платежных систем (YooKassa/Robokassa) **обязательно** требуется HTTPS.

**Шаг А: Получение сертификата (на хосте)**
Остановите Nginx, установите Certbot и получите сертификат:
```bash
docker-compose stop nginx
sudo apt update && sudo apt install certbot -y
sudo certbot certonly --standalone -d bluestats.khodzhji.beget.tech
```

**Шаг Б: Настройка переменных окружения**
В корневом файле `.env` (рядом с `docker-compose.yml`) укажите, какой конфиг Nginx использовать:
```env
# Использовать production.conf для HTTPS (по умолчанию используется default.conf)
NGINX_CONF=production.conf
```

**Шаг В: Перезапуск Docker**
```bash
docker-compose up -d --force-recreate
```

### 2. Платежные системы
В файле `laravel/.env` должны быть заполнены следующие поля:

**YooKassa:**
- `PAYMENT_GATEWAY=yookassa`
- `YOOKASSA_SHOP_ID` (из личного кабинета)
- `YOOKASSA_SECRET_KEY` (полный ключ **без звездочек**)

**Webhook URL для YooKassa:**
`https://ваш-домен.ru/api/payments/callback/yookassa`

---

## 🛠 Технические особенности

### Умный редирект (Smart Polling)
После оплаты в YooKassa пользователь попадает на маршрут `/payment/check/{id}`. Сервер делает 4 попытки (каждые 0.5 сек) проверить наличие вебхука в базе, прежде чем отправить пользователя на страницу успеха или ошибки. Это сглаживает сетевые задержки.

### Инфраструктура
- **Docker**: PHP 8.4-FPM, PostgreSQL 15, Nginx.
- **SSL**: Монтируется из `/etc/letsencrypt` хостовой машины прямо в контейнер Nginx в режиме `readonly`.
- **Rate Limiting**: Для API (включая вебхуки) настроен лимитер `60 запросов/мин`.

# ARM Sales - Система управления продажами

## 📋 Описание

ARM Sales - это автоматизированная система управления продажами для компаний, работающих с CRM BlueSales. Система включает управление клиентами, сделками, заказами, рабочими сессиями и AI-аналитику эффективности отдела продаж.

## 🏗️ Архитектура проекта

```
ARM_sales/
├── laravel/                 # Laravel приложение
│   ├── app/                 # Код приложения (модели, контроллеры, сервисы)
│   ├── database/            # Миграции и сидеры
│   ├── public/              # Веб-корень (статические файлы)
│   ├── resources/           # Views, CSS, JS исходники
│   ├── routes/              # Маршруты приложения
│   ├── composer.json        # PHP зависимости
│   └── .env                 # Laravel конфигурация
├── docker/                  # Docker конфигурации
│   ├── nginx/               # Nginx настройки и конфигурация
│   ├── php/                 # PHP-FPM Dockerfile и настройки
│   └── postgres/            # PostgreSQL инициализация
├── docker-compose.yml       # Docker Compose конфигурация
├── deploy.sh               # Скрипт автоматического развертывания
├── Makefile               # Команды управления проектом
├── .env                   # Docker окружение
└── README.md              # Этот файл
```

## 🛠️ Технологический стек

- **Backend:** PHP 8.2, Laravel 12.0
- **Database:** PostgreSQL 15
- **Frontend:** HTML5, CSS3, JavaScript, TailwindCSS 4.0
- **Admin Panel:** Filament 3.3
- **Containerization:** Docker, Docker Compose
- **Web Server:** Nginx (Alpine)
- **Cache/Queue:** Redis 7
- **AI Integration:** Google Gemini API (через ProxyAPI)
- **CRM Integration:** BlueSales API

## 🚀 Быстрый старт

### Требования
- VPS с Ubuntu 22.04 LTS
- Docker CE и Docker Compose
- Минимум: 1 CPU, 2GB RAM, 15GB SSD

### 1. Клонирование проекта
```bash
git clone <repository-url>
cd ARM_sales
```

### 2. Автоматическое развертывание
```bash
chmod +x deploy.sh
./deploy.sh
```

### 3. Альтернативный способ (через Makefile)
```bash
make setup  # Первоначальная настройка
make demo   # Полное развертывание с демо-данными
```

### 4. Доступ к приложению
- **Веб-интерфейс:** http://your-server-ip
- **Админка Filament:** http://your-server-ip/admin

## 👥 Демо-аккаунты

| Роль | Email | Пароль | Права доступа |
|------|-------|---------|---------------|
| Руководитель | head@demo.com | password | Полный доступ + AI-аналитика + планирование |
| Менеджер | manager@demo.com | password | Клиенты, сделки, задачи, рабочие сессии |
| Админ | admin@demo.com | password | Filament админка для управления пользователями |

## ⚡ Основные функции

### 🔐 Система ролей и безопасность
- Авторизация с ролями (руководитель, менеджер, админ)
- Контроль рабочего времени (старт/стоп сессий)
- Политики доступа для каждой сущности

### 👥 Управление клиентами
- CRUD операции с клиентской базой
- Интеграция с BlueSales CRM (синхронизация данных)
- Расширенные поля (статус CRM, источники, теги, контакты)
- История взаимодействий

### 💼 Сделки и заказы
- Управление сделками с различными статусами
- Автоматический импорт заказов из BlueSales
- Связь заказов со сделками и клиентами
- Отчеты по времени и эффективности

### 📊 Планирование и задачи
- Создание и назначение задач сотрудникам
- Установка месячных и дневных планов продаж
- Отслеживание выполнения планов
- Календарное планирование

### 🤖 AI-аналитика
- Автоматическая генерация еженедельных отчетов
- Анализ эффективности отдела продаж
- Рекомендации по улучшению работы
- Анализ конверсии лидов в заказы

## 🔧 Управление проектом

### Основные команды
```bash
# Управление сервисами
make up         # Запуск всех сервисов
make down       # Остановка сервисов
make restart    # Перезапуск
make logs       # Просмотр логов

# Laravel команды
make migrate    # Выполнить миграции
make seed       # Заполнить тестовыми данными
make cache      # Очистить кеши
make shell      # Доступ к PHP контейнеру

# Разработка
make clean      # Очистка Docker кеша
make build      # Пересборка контейнеров
```

### Docker Compose команды
```bash
# Статус контейнеров
docker-compose ps

# Логи конкретного сервиса
docker-compose logs -f nginx
docker-compose logs -f php
docker-compose logs -f postgres

# Выполнение Laravel команд
docker-compose exec php php artisan migrate
docker-compose exec php php artisan queue:work
docker-compose exec php composer install
```

## 📈 Мониторинг и оптимизация

### Проверка ресурсов
```bash
# Статистика Docker контейнеров
docker stats

# Использование диска
df -h

# Мониторинг памяти
free -h
```

### Оптимизация для ограниченных ресурсов
- Настроенный OPcache для ускорения PHP
- Оптимизированные настройки PostgreSQL для 2GB RAM
- Gzip сжатие в Nginx
- Кеширование статических файлов

## 🔧 Настройка AI-аналитики

1. Получите API токен от [ProxyAPI.ru](https://proxyapi.ru)
2. Добавьте в `laravel/.env`:
   ```env
   AI_API_TOKEN=your_actual_token_here
   ```
3. Перезапустите контейнеры: `make restart`

## 🔒 Безопасность

- Смените пароли БД в `.env` для продакшена
- Настройте файрвол на VPS
- Используйте SSL сертификаты
- Регулярно обновляйте Docker образы
- Ограничьте SSH доступ

## 📚 Документация

- **[DEPLOYMENT.md](DEPLOYMENT.md)** - Подробная инструкция по развертыванию
- **[coursework.md](laravel/coursework.md)** - Техническое описание проекта
- **Laravel документация:** https://laravel.com/docs
- **Filament документация:** https://filamentphp.com/docs

## 🆘 Troubleshooting

### Частые проблемы

1. **Контейнер не запускается:**
   ```bash
   docker-compose logs <service_name>
   make clean && make build
   ```

2. **Ошибка подключения к БД:**
   ```bash
   make restart
   # Проверить статус PostgreSQL
   docker-compose logs postgres
   ```

3. **Нехватка памяти:**
   ```bash
   # Добавить SWAP
   sudo fallocate -l 2G /swapfile
   sudo mkswap /swapfile
   sudo swapon /swapfile
   ```

4. **AI-аналитика не работает:**
   - Проверить AI_API_TOKEN в `laravel/.env`
   - Проверить доступ к интернету из контейнера

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи: `make logs`
2. Изучите документацию в DEPLOYMENT.md
3. Проверьте требования к системе
4. Убедитесь в корректности .env файлов

---

**Версия:** 1.0  
**Лицензия:** MIT  
**Автор:** ARM Sales Development Team